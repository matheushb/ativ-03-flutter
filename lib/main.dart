import 'package:flutter/material.dart';
import 'package:form/components/form-trab.dart';

void main() {
  runApp(MaterialApp(
    title: 'joao',
    home: Scaffold(
      appBar: AppBar(
        title: const Text('joao'),
      ),
      body: FormTrab(),
    ),
  ));
}
